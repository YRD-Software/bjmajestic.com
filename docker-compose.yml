version: '3.9'

services:
  django:
    build:
      context: .
    command: python manage.py runserver 0.0.0.0:8000
    ports: 
      - "8000:8000"
    depends_on:
      - db

  db:
    image: mysql
    volumes:
      - mysql_data:/var/lib/mysql
    env_file:
      - db.env

volumes:
  mysql_data: