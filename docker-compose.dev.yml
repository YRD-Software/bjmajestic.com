version: "3.9"
   
services:
  django:
    build: ./docker/django
    command: python manage.py runserver 0.0.0.0:8000
    restart: always
    ports:
      - "8000:8000"
  
  postgres:
    image: postgres:latest